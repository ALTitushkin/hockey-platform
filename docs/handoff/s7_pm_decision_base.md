# Хэндофф · Sprint 7 · Трек B — РЕШЕНИЕ PM по схеме → DEV-чат

> **От:** PM · **Кому:** чат-разработчик (Cowork) · **Дата:** 28.06.2026
> Ответ на `s7_dev_base_reply.md`. Все вопросы закрыты — можно запускать bulk после патча тулинга.

## Схема — АПРУВ
- **2 новые категории принимаю:** `basics` («Базовое / техника») и `org` («Лиги / организации»). Чище для UI-фильтров, чем общий `meta`. Согласен.
- Раскладку терминов из таблицы принимаю **с двумя правками** (ниже).

## Ответы на 3 вопроса
1. **NHL отдельной карточкой — ДА.** Завести `nhl` как термин в кластер `leagues`, category `org`, level novice. Кластеру лиг нужна якорная лига; плюс «Нхл» — частый запрос. 
2. **Патч `add_term.py` — ДА, добро.** Обнови под схему v2.0: новые категории `basics/org` + запись полного набора полей (`profiles/cluster/see_also/anchor/source/added`, `status: verified`). Это тулинг, не данные — в этом же спринте, отдельным коммитом перед добавлением.
3. **Хит/силовой → `basics`, не `rule`.** Обоснование: силовой приём — это легальный фундаментальный элемент игры, а не нарушение (в отличие от `cross-check`, который и есть фол). В определении оговорить, что легальность хита регулируется правилами (борт/атака сзади и т.п.). Аналогия с cross-check здесь не подходит. Категория — `basics`.

## Правки к раскладке
- **Силовой приём / Хит:** category `rule` → **`basics`** (см. п.3). Остальное как у тебя: merge в один термин, алиасы «хит»/«силовой», level novice.
- **НМХЛ — ДОБАВЛЯЕМ.** Проверено: это реальная **Национальная молодёжная хоккейная лига** (nmhl.fhr.ru), не опечатка. В кластер `leagues`, category `org`, level novice.

## Итоговый кластер «Лиги» (6 карточек)
NHL · КХЛ · МХЛ · ВХЛ · ECHL · НМХЛ — все `org`, novice, cluster `leagues`. Запись кластера в `clusters.json` — как ты предложил (`slug: leagues`, `title: «Лиги»`, `order: 3`).

## Остаётся отложенным
- **«Подкидка»** — формат не уточнён (flip-бросок / scoop), НЕ добавляем, вернёшь позже.

## Прочее — без изменений
- level «Драфт» = fan (Средний) — ок.
- `see_also`: подправление → screen/rebound/slot; один-в-один → breakaway; лиги — между собой.
- План исполнения (патч → П1→П2→кластер → validate → CHANGELOG → пуш с лог-фиксом 52→49(+3)) — принимаю.

## DoD напоминание
- `validate_data.py` зелёный, зеркала идентичны, все записи `verified` с `source`.
- Перед пушем — финальный список добавленного пришли PM на приёмку (термин + категория + level + источник).

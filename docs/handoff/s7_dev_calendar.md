# Хэндофф · Sprint 7 · Трек A — бот «📅 В этот день» → DEV-чат

> **От:** PM · **Кому:** чат-разработчик (Cowork) · **Дата:** 28.06.2026
> **Скоуп:** ядро S7 — историческая рубрика в боте на готовых данных календаря.
> Полная спека — `docs/sprint_7.md` (раздел «Что входит», A1–A5). Этот файл — точка входа + границы.

## Контекст
Низкорисковая фича: читаем готовый JSON, без внешних API и LLM в рантайме. Готовит почву под будущий scheduled task «саммари дня в канал».

## Источник данных
- Курируемый календарь: `hockey_calendar/hockey_calendar.json` (101/366 дней, 119 событий, video пока 0).
- Завести `data/calendar.json` в репо (ключ `MM-DD` → массив событий). Поля: `year`, `text`, `cat`, `reg`, `conf`, `video` (опц.). Схема — в `sprint_7.md`.
- Наполнение календаря и `video` — отдельный поток исследователя (`hockey_calendar/PROMPT_for_Researcher.md`), бот работает на любом срезе.

## Задачи (A1–A5, детали в sprint_7.md)
1. `data/calendar.json` (+ зеркало `docs/data/`, если того требует validate-паритет) — валидируется загрузчиком.
2. `calendar.py` (или в `search.py`): `load_calendar()`, `events_for_date("MM-DD")`, `parse_date(text)` — форматы «2 января»/«2 янв»/«02.01»/«14-03», игнор регистра/мусора, TZ Europe/Moscow.
3. Кнопка «📅 В этот день» в `/start` (`callback_data="onthisday:today"`) + `/today` + `/day <дата>`. В `handle_query` прогонять `parse_date()` ДО словаря; распознанную дату не писать в `missing_queries.log`.
4. Карточка дня (HTML, как `format_card`): заголовок «📅 <b>14 марта</b> в истории хоккея», по событию `<b>{year}</b> — {text}` + метка `cat`, при `video` — «🎥 Смотреть момент», пустая дата → дружелюбная заглушка.
5. `test_calendar.py` без токена (форматы дат, события, сортировка, пустая дата). CHANGELOG → v0.10, README/CLAUDE.md.

## Грабли (учесть — из прошлых спринтов)
- PTB запинен `<22` — не обновлять (22.x ломает `run_polling`).
- ⚠️ Антиколлизия со словарём: реагируем как на дату ТОЛЬКО при явном дате-паттерне (число + месяц/разделитель). Одиночные слова («март») датой не считаем.
- После деплоя проверять `journalctl` на строку «Бот запущен …», не только `is-active` (был кейс обрезанного bot.py).
- Пуш заденет `bot/**` и `data/**` → автодеплой бота. Сюда же удобно вложить мелочь: лог `терминов: 52` → `49 (+3 на выверке)`.

## Definition of Done (Трек A)
- [ ] `data/calendar.json` в репо, грузится валидатором; схема включает `video`.
- [ ] `parse_date` понимает `DD месяц` / `DD.MM` / `MM-DD`; без ложных срабатываний на одиночных словах.
- [ ] «📅 В этот день» + `/today` + `/day <дата>` работают на проде.
- [ ] Запрос даты текстом → карточка, не пишется в `missing_queries.log`.
- [ ] Пустая дата → заглушка (не падение). `test_calendar.py` зелёный.
- [ ] CHANGELOG v0.10, README/CLAUDE.md; деплой проверен по `journalctl`.

## Границы
- Не наполнять сам календарь (это исследователь). Не делать авто-синк/scheduled task/inline для дат — это бэклог S8+.

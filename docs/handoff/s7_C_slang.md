# Хэндофф · Sprint 7 · Трек C — чистка MT-сленга + ясность подписи → DEV + ЭКСПЕРТ

> **От:** PM · **Дата:** 28.06.2026
> Источник: фидбэк юзера из лога («палка/кругляш/таблетка никто не говорит», «что значит „у нас говорят“?»).
> Аудит: 33 термина с `ru_slang`. Поле перегружено — в нём вперемешку сленг, синонимы, переводы и алиас.
> Маршрут: часть чинит дев сразу (юзер-подтверждённое + структура), часть ждёт выверки эксперта СКА.

## Корневая проблема
Подпись в боте (`search.py:111`): `💬 У нас говорят: {ru_slang}`. Но `ru_slang` содержит 4 разных типа контента → показывает не-сленг как «народное название», отсюда путаница и кальки.

---

## A. Убрать СЕЙЧАС (дев) — кальки машинного перевода, юзером подтверждено «никто не говорит»
- **Шайба (puck):** убрать «кругляш, таблетка».
- **Клюшка (hockey-stick):** убрать «палка».
→ Если реального сленга не остаётся — поле `ru_slang: null` (не выдумывать замену).

## B. Перенести/почистить СЕЙЧАС (дев) — это НЕ сленг, лежит не в том поле
- **РХЛ:** «НМХЛ» — это алиас, уже есть в `abbr`. Из `ru_slang` убрать.
- **Проброс (icing):** «проброс» — дубль самого термина → `null`.
- **Офсайд (offside):** «вне игры» — это перевод, не сленг → `null` (или в `notes`).
- **Выход из зоны (breakout):** «выход из зоны» — дубль термина → `null`.
- **Хват (handedness):** «левый хват, правый хват» — это варианты, не сленг; уже алиасы поиска → из `ru_slang` убрать.

## C. На выверку ЭКСПЕРТУ СКА (не трогать, пока не подтвердит) — сомнительное
- **Чекер (checker):** «гасильщик» — реальное слово или калька?
- **Отскок (rebound):** «подбор» (заимствование из баскетбола?) — насколько в ходу.
- **Кистевой щелчок (snap-shot):** «подщёлкивание».
- **Тафгай (enforcer):** «полицейский» — подтвердить.
- Заодно по `expert_review.json` (давно висит `unverified`): **рыбачок, поплавок, сыр/в девятку** — подтвердить или отклонить.

## D. Оставить (дев не трогает) — похоже на настоящий разговорный сленг
пятак · сухарь · точка · дэф · кипер/голи · тафгай · трафик · вантаймер · трап/капкан · вброс · центр · крайний/вингер · хит · карусель/возить · добивание · сухарь/на ноль и т.п. (полный список — в данных). Эти оставляем как есть.

---

## Подпись «у нас говорят» — решение PM нужно (3 варианта)
Юзер прямо спросил «что это значит». Варианты:
1. Переименовать в нейтральное: **«Сленг:»** или **«Также называют:»** (рекомендую «Сленг:» — честно и коротко).
2. Показывать строку ТОЛЬКО когда в `ru_slang` реально сленг (после чистки A/B), иначе скрывать.
3. Оставить как есть (не рекомендую — путает).
→ Рекомендую 1 + 2 вместе: переименовать в «Сленг:» И скрывать, если пусто.

## Порядок
1. Дев: A + B + подпись (после решения PM по варианту) — это не ждёт эксперта, чистит юзер-подтверждённое и структуру. Пуш (задевает data/** + bot/**).
2. Эксперт СКА: выверка C + expert_review.json — асинхронно (контакт у Саши).
3. Дев: по итогам эксперта — финальные правки (подтверждённое → оставить/перенести в базу, отклонённое → убрать).

## DoD Трека C
- [ ] Кальки (палка/кругляш/таблетка) убраны; не-сленг (B) вычищен из `ru_slang`.
- [ ] Подпись бота приведена к решению PM; пустой `ru_slang` не показывается.
- [ ] Список C отправлен эксперту; ответ применён (или зафиксирован как ожидание).
- [ ] `validate_data.py` зелёный, паритет ок, тесты зелёные.

# Линтер/форматтер (ruff). Общий для bot/ и tools/.
target-version = "py311"
line-length = 100

[lint]
# E — стиль/ошибки, F — pyflakes (мёртвые импорты/переменные), I — сортировка импортов.
select = ["E", "F", "I"]
# При созревании расширить: "UP" (модернизация синтаксиса), "B" (баг-паттерны).
